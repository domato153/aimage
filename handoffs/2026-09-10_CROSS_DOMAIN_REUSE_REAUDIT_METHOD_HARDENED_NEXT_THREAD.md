# AIMAGE Next-Thread Handoff — Cross-Domain Reuse Re-audit, Method-Hardened

Packet status: **ACTIVE continuity evidence only after adoption and fresh reconciliation**. This packet is intended to supersede `handoffs/2026-09-10_CROSS_DOMAIN_REUSE_REAUDIT_NEXT_THREAD.md` once current AIMAGE authority adopts it.

## Operational Header

- **Canonical packet locator:** current `governance/continuity/CURRENT.md` must point to the exact immutable commit that first contains this finalized packet.
- **Immediate objective:** before native Engine/Core design, re-audit every material `DESIGN`/partial-`DESIGN` capability against mature non-image-domain solutions.
- **Phase:** planning / design / research only.
- **Stop boundary:** no engine/provider implementation, package installation, source import, or runtime dependency change during this slice.
- **Urgency / deadline:** no material external deadline, response window, or external ownership wait is known at construction. A newer explicit user instruction may change this after fresh reconciliation.
- **Exact next bounded action:** complete the capability-first cross-domain reuse re-audit and update the accepted planning surfaces so only irreducible AIMAGE-native semantics remain.
- **Completion criterion:** every materially `DESIGN`/partial-`DESIGN` row has an evidence-backed classification and acquisition boundary; every retained `DESIGN` has explicit negative evidence; no unresolved candidate class remains that could plausibly change a material classification; workflow simplifications have been assessed; `CURRENT.md` records exactly one following action.
- **Stop / replan:** stop if fresh authority supersedes this packet, the user changes phase/reuse policy, research invalidates the product boundary itself, licensing/runtime constraints force a materially different architecture, or incompatible high-level architectures remain beyond this bounded audit.

## Identity / locator

- Repository: `domato153/aimage`
- Construction branch: `aimage-stage/handoff-external-method-hardening`
- Path: `handoffs/2026-09-10_CROSS_DOMAIN_REUSE_REAUDIT_METHOD_HARDENED_NEXT_THREAD.md`
- Construction base `main`: `877ce7dc7c48c1f0976a4ec926ece81872a1feb7`
- Governing handoff method at construction: `.agents/skills/handoff/SKILL.md` on this candidate branch after external-method hardening
- Governing continuity method: `governance/CONTINUITY.md`
- Applicable profile: `governance/continuity/REPOSITORY_WORK.md`
- External-method audit: `audits/HANDOFF_EXTERNAL_METHOD_REAUDIT_2026-09-10.md`

Fresh live AIMAGE authority outranks all recorded literals if they later move.

## Material delta from predecessor

This packet supersedes the prior cross-domain packet only after adoption. The project objective is unchanged; the handoff method and finish controls are stronger.

Material differences:

1. **Completion/acceptance criteria are first-class.** The receiver must know exactly when the research/reclassification slice is finished.
2. **Research saturation is bounded.** The goal is defensible classification, not exhaustive enumeration of every project in every adjacent field.
3. **Operational control state is top-scannable.** Objective, phase, next action, finish condition and stop trigger are in the Operational Header.
4. **Context filtering is explicit.** Durable AIMAGE state stays in its owning files; this packet transfers only decision-relevant interpretation/delta.
5. **Receiver synthesis functions as read-back.** Before `ACCEPTED`, the receiver must restate objective, one next action, completion criterion and stop/replan trigger.
6. **Timing/ownership is explicit when material.** No material deadline/external wait is currently known; do not invent one.

The predecessor's core cross-domain reuse decision and provider-native repair decision remain preserved.

## Immediate objective

Before AIMAGE designs any remaining Image Engine/Core capability, perform a **capability-first cross-domain reuse re-audit**.

For each capability currently classified as `DESIGN` or partly `DESIGN`, translate the AIMAGE term into the underlying abstract problem and search mature fields outside image generation for standards, libraries, architectures, protocols, data models and production methods that already solve that problem.

The target is not “find one external project identical to AIMAGE.” A valid result may compose several mature primitives and leave only a thin AIMAGE semantic residue.

## Current phase and authorized scope

Phase: **planning / design / research only**.

Authorized:

- fresh external research;
- standards/architecture comparison;
- capability reclassification;
- planning-document repair;
- evidence-backed workflow simplification;
- continuity update;
- bounded branch/PR review and adoption of the planning result.

Not authorized merely by this packet:

- Image Engine/provider implementation;
- package installation;
- external source-code import;
- runtime dependency changes;
- production code adoption.

## Urgency / time / commitments

No material deadline, external response window, promised delivery date, or external-party wait is known at packet construction.

If a newer user instruction introduces one, fresh user instruction governs intent/timing after reconciliation. Do not infer urgency from the existence of this handoff.

## Fresh authority snapshot at construction

Decision-relevant accepted state at construction base:

- `main`: `877ce7dc7c48c1f0976a4ec926ece81872a1feb7`
- root router: `AGENTS.md`
- continuity: `governance/CONTINUITY.md`
- repository profile: `governance/continuity/REPOSITORY_WORK.md`
- current state: `governance/continuity/CURRENT.md`
- architecture boundary: `architecture/BOUNDARIES.md`
- workflow target: `plans/WORKFLOW_TARGET.md`
- capability matrix: `plans/CAPABILITY_REUSE_MATRIX.md`
- external reuse plan: `plans/EXTERNAL_REUSE_PLAN.md`
- predecessor packet: `handoffs/2026-09-10_CROSS_DOMAIN_REUSE_REAUDIT_NEXT_THREAD.md`
- predecessor handoff audit: `audits/NEXT_THREAD_HANDOFF_REAUDIT_2026-09-10.md`

The new handoff-method hardening exists on the construction branch until adopted. Its candidate existence does not authorize itself.

## Current situation

### Confirmed

1. AIMAGE already has accepted Continuity/Handoff infrastructure, thin Project bootstrap, root AGENTS routing, architecture boundaries, workflow target, initial capability reuse matrix and external acquisition policy.
2. The accepted product policy is evidence-first `ADOPT / ADAPT / DESIGN`: reuse mature external capability where it closes the requirement and design only the genuine remainder.
3. Initial research focused too heavily on image-generation projects and likely overclassified generic engineering/creative-production problems as AIMAGE-native design.
4. A second re-audit already changed the next action to capability-first **cross-domain** research rather than native core design.
5. Actual targeted/delta/masked/multi-turn image editing should be executed by providers/tools when suitable. AIMAGE should not build a custom partial-image repair renderer. Its possible role is higher-level diagnosis, preservation intent, capability selection/orchestration and re-review.
6. External high-reliability handoff methods were subsequently reviewed. The AIMAGE handoff method was strong but needed explicit finish criteria, material timing/ownership, predecessor delta, top-level operational scanability, filtered context and stronger receiver read-back.

### Supported hypothesis to challenge

Several current `DESIGN` classifications can likely become `ADOPT`, `ADAPT`, or `ADAPT + small AIMAGE extension`. Even apparently project-specific semantics must survive the AIMAGE-specific value test rather than being assumed necessary.

## Governing research method

For **every materially `DESIGN` or partial-`DESIGN` capability**:

1. translate the AIMAGE term into the **abstract problem**;
2. identify mature non-image domains that solve the same problem class;
3. search authoritative standards/specifications and primary documentation first where available;
4. inspect mature maintained implementations and established production patterns;
5. use technical/academic literature when it materially helps resolve design tradeoffs;
6. treat proprietary systems as reference-only evidence when useful;
7. identify the strongest candidates, not every candidate;
8. test direct `ADOPT`, `ADAPT`, method/schema reuse, interoperability and **composition of multiple mature solutions**;
9. retain native `DESIGN` only for the smallest exact AIMAGE semantic residue not adequately closed by those routes.

A lack of software using AIMAGE's exact vocabulary is not evidence that the underlying problem is unsolved.

## Cross-domain discovery families

The receiver is not restricted to these examples. Search the mature field corresponding to each abstract capability.

### Workflow / state / history / human gates

Consider:

- durable workflow systems;
- statecharts/state machines;
- BPMN/user-task patterns;
- event sourcing / append-only history;
- saga/compensation patterns where relevant.

Challenge whether AIMAGE needs native lifecycle machinery or only domain state over mature workflow primitives.

### Artifact identity / version / lineage / provenance

Consider:

- DAM/MAM;
- OpenAssetIO or equivalent media-asset integration boundaries;
- version control;
- content-addressed storage;
- W3C PROV;
- build/supply-chain provenance;
- C2PA only where exported-content provenance is relevant.

Separate internal workflow lineage from optional externally attestable media provenance.

### Geometry / composition / spatial constraints

Consider:

- OpenUSD and other VFX/DCC scene graphs;
- CAD constraint systems;
- game-engine transforms/scene graphs;
- UI layout/constraint solvers;
- 2D spatial-relation models.

Challenge whether relations such as `same row`, `right of`, `slightly behind`, `distance`, `alignment`, `gaze`, camera and reading order can reuse established constraint/scene primitives.

### RenderSpec / provider lowering

Treat the problem abstractly as an **intermediate representation / job specification / execution plan** problem.

Consider:

- compiler IRs;
- build systems;
- query planners;
- job/execution specifications;
- validation/normalization/lowering architectures.

Challenge whether AIMAGE needs only a thin semantic IR compiled into provider-specific requests.

### Provider adapters / capability negotiation

Consider:

- ports-and-adapters / hexagonal architecture;
- plugin systems;
- device-driver models;
- protocol feature negotiation;
- hardware/API capability descriptors;
- compatibility/version negotiation.

Do not invent provider machinery merely because providers expose different APIs.

### Reference authority / partial approval / locks / overrides

Consider:

- DCC/CAD layer locking;
- non-destructive overrides;
- layered configuration;
- policy/constraint systems;
- creative-review/version workflows;
- selective approval/staging patterns.

Challenge whether the only AIMAGE-specific residue is a thin mapping from user intent to visual dimensions and cross-provider preservation behavior.

### Persistence / schema / configuration

Consider:

- schema evolution;
- versioned serialization;
- configuration overlays;
- project-state persistence;
- immutable snapshots / content-addressed artifacts.

Do not choose a database before semantic boundaries are known.

### Repair planning / orchestration

Treat actual editing as provider/tool execution when available.

Consider analogues in:

- diagnosis -> planning -> executor separation;
- fault localization;
- minimum-change repair;
- transaction compensation;
- tool orchestration / planners.

Possible AIMAGE responsibility is only what is necessary to:

1. diagnose failed variable(s);
2. identify protected/successful elements;
3. select the cheapest suitable provider/tool capability;
4. express the delta/preservation intent;
5. invoke through an adapter;
6. re-review for success/regression.

Even those semantics must be tested against mature external patterns.

## Existing named evidence targets

Freshly verify relevant candidates; none is a predetermined answer:

- InvokeAI;
- OpenAssetIO;
- W3C PROV;
- OpenUSD;
- Temporal or equivalent durable-workflow systems;
- OpenAI/GPT Image editing capabilities;
- ComfyUI;
- Diffusers;
- Nori;
- GenAI Illustration Pipeline;
- Krita AI Diffusion patterns;
- style-consistency-ai patterns.

For an accepted external candidate, determine acquisition mode, version/currentness, license/provenance, runtime/source coupling, AIMAGE owner boundary and replacement/fallback behavior.

## DESIGN burden of proof

A material capability may remain `DESIGN` only if the planning record states:

- abstract problem;
- relevant fields/standards/implementations searched;
- strongest candidates;
- why `ADOPT` does not adequately close it;
- why `ADAPT` does not adequately close it;
- why composition of multiple mature primitives does not adequately close it;
- smallest exact AIMAGE-owned semantic residue.

“Nothing exactly called this exists” is insufficient.

## AIMAGE-specific value test

For every proposed AIMAGE-owned semantic ask:

> What user-visible or cross-provider control would actually be lost if AIMAGE used mature external primitives directly instead?

If the answer is only naming, wrapper convenience, preferred storage shape or duplication, remove it from native core design.

Possible legitimate residues — still hypotheses — include:

- user-intent authority across visual dimensions;
- partial-dimension approval/lock meaning;
- provider-neutral preservation intent;
- capability-aware cross-provider orchestration.

None is exempt from the reuse audit.

## Context filtering / durable-state separation

This packet does not duplicate the contents of the workflow/matrix/reuse plans. Those files remain the owning durable state.

The receiver must fresh-read them and use this packet only for:

- current objective;
- why the next action changed;
- preserved decisions/negative boundaries;
- research method;
- finish condition;
- stop/replan boundary.

Do not treat this packet as a replacement for current plan files or copy their entire contents into a derived prompt.

## Completed scope

Do not restart without new evidence:

- Continuity adoption;
- maintenance-derived Handoff adoption;
- thin GPT Project bootstrap;
- root AGENTS routing;
- initial architecture boundary;
- initial workflow target;
- initial capability reuse matrix;
- initial external acquisition plan;
- first next-thread reuse handoff;
- cross-domain successor handoff/audit;
- decision that provider-native targeted edit execution should be reused rather than replaced by a custom AIMAGE renderer.

The active work is only the cross-domain re-audit of the remaining native-design surface.

## Preserved decisions / negative boundaries

- Reuse mature capability first; design only the remainder.
- Search by abstract problem, not exact AIMAGE terminology.
- Do not restrict research to image-generation software or a fixed candidate list.
- Do not require one external project to solve the entire capability; composition is valid.
- Do not blindly copy whole external repositories.
- External systems remain evidence/dependencies behind AIMAGE boundaries, not AIMAGE authority.
- Do not make provider/tool representations canonical AIMAGE state merely for convenience.
- Do not copy GPL/noncommercial source into a permissive core without an explicit licensing decision.
- Do not infer model/checkpoint/content licenses from library licenses.
- Do not build a custom image editor/repair executor where provider/tool execution suffices.
- Do not collapse AIMAGE into a thin single-provider wrapper.
- Keep Continuity/Handoff outside Image Engine product semantics.
- Keep `features/domain capabilities -> engine interfaces` unless an explicitly accepted architecture revision changes it.
- Keep trivial one-shot image generation simple.
- Do not implement providers/core during this planning slice.

## Decision-relevant dependencies / artifacts

All required continuation state is intended to be durable in `domato153/aimage` after this packet and method hardening are adopted.

No required chat-only image, local file, temporary URL or external-party response is known.

Fresh-check only bounded related work touching this same handoff/reuse boundary; do not crawl unrelated branches.

## Exact next bounded action

**Re-audit every materially `DESIGN` or partial-`DESIGN` capability in `plans/CAPABILITY_REUSE_MATRIX.md` using capability-first cross-domain research outside image generation; update `plans/CAPABILITY_REUSE_MATRIX.md` and `plans/EXTERNAL_REUSE_PLAN.md` with evidence-backed reclassifications and acquisition boundaries, simplify `plans/WORKFLOW_TARGET.md` only where the same evidence proves a stage redundant, and update `governance/continuity/CURRENT.md` so only the irreducible AIMAGE-native semantic residue remains for the following design slice.**

Why this is next: proceeding directly into native contract design risks freezing abstractions for problems that mature non-image fields already solve.

## Completion / acceptance criteria

The bounded re-audit is complete when **all** of the following hold:

1. every current materially `DESIGN` or partial-`DESIGN` row has been explicitly accounted for;
2. each accounted row has an abstract-problem statement and bounded cross-domain search surface;
3. the strongest materially relevant standards/tools/patterns found are recorded with sufficient current evidence to support the classification;
4. each row has a defensible resulting classification: `ADOPT`, `ADAPT`, `ADAPT + small AIMAGE extension`, or `DESIGN`;
5. each accepted external route has an acquisition/integration boundary, with license/provenance/runtime coupling and replacement/fallback noted where material;
6. every retained `DESIGN` satisfies the full DESIGN burden of proof and states the smallest irreducible AIMAGE residue;
7. no **known unresolved candidate class** remains that is plausibly capable of materially changing one of those classifications;
8. `WORKFLOW_TARGET.md` has been checked for evidence-backed stage collapse/optionalization/delegation, whether or not it actually changes;
9. the resulting planning candidate has been independently checked against fresh `main` and `architecture/BOUNDARIES.md`;
10. the accepted result, if adopted, updates `CURRENT.md` with exactly one following action.

### Research saturation / exit rule

Do **not** attempt an exhaustive survey of every implementation in every adjacent domain.

Evidence is sufficient when the receiver has covered the mature **problem classes** capable of changing the decision, identified the strongest representative standards/implementations/patterns for those classes, and no unresolved class remains with a plausible path to changing a material classification.

If two or more candidates are equivalent for the planning decision, record the representative strongest options and stop unless choosing among them is itself necessary for the next action.

The finish condition is **decision sufficiency**, not maximal bibliography size.

## Expected transition

A successful next thread leaves:

- a materially narrower and defensible native `DESIGN` set;
- explicit evidence/negative evidence supporting the remaining set;
- accepted cross-domain reuse candidates and acquisition boundaries;
- provider-native image editing clearly separated from AIMAGE repair orchestration;
- any evidence-backed workflow simplifications;
- an updated `CURRENT.md` with exactly one next action aimed only at genuinely remaining AIMAGE-specific semantics, unless the audit shows a different architecture direction is warranted.

## Stop / replan conditions

Return `STALE_REPLAN` or stop before mutation if:

- current AIMAGE authority materially moved or a newer accepted handoff/reuse audit supersedes this route;
- current governing handoff/continuity method materially changes the receiver procedure;
- the user changes phase or reuse-first policy;
- research invalidates the product/workflow boundary itself rather than merely shrinking native design;
- licensing/runtime/distribution constraints force a materially different high-level architecture;
- multiple incompatible high-level architectures remain and selecting among them exceeds this bounded audit.

## Cold-start read route

A zero-chat receiver should read in this order:

1. fresh current `main` and root `AGENTS.md`;
2. current `.agents/skills/handoff/SKILL.md`;
3. current `governance/CONTINUITY.md`;
4. current `governance/continuity/REPOSITORY_WORK.md`;
5. this packet from the exact immutable locator recorded by current `governance/continuity/CURRENT.md`;
6. `audits/HANDOFF_EXTERNAL_METHOD_REAUDIT_2026-09-10.md` only if the external-method hardening rationale is needed; it is audit evidence, not runtime authority;
7. current `governance/continuity/CURRENT.md`;
8. current `architecture/BOUNDARIES.md`;
9. current `plans/WORKFLOW_TARGET.md`;
10. current `plans/CAPABILITY_REUSE_MATRIX.md`;
11. current `plans/EXTERNAL_REUSE_PLAN.md`;
12. bounded discovery for newer work touching this same reuse/design/handoff boundary.

Then independently re-derive the minimum dependency set and challenge the packet.

## Hard prohibitions

- Do not begin provider/core implementation.
- Do not search only image-generation projects.
- Do not search only the named candidate list.
- Do not retain `DESIGN` because terminology differs.
- Do not require a single upstream solution before crediting reuse.
- Do not turn this into an exhaustive unbounded literature/project survey after the completion criteria are satisfied.
- Do not treat provider edit execution as AIMAGE authority/approval semantics.
- Do not invent a custom repair renderer when existing provider/tool execution suffices.
- Do not copy restricted-license source without an explicit compatible licensing decision.
- Do not reopen Continuity/Handoff unless fresh evidence identifies an actual defect.

## Receiver synthesis / read-back and acceptance

Before consequential continuation, the receiver must be able to state in ordinary language:

- **Objective:** minimize remaining AIMAGE-native design through a cross-domain reuse re-audit.
- **One next action:** re-audit and update the reuse/planning surfaces, not implement the engine.
- **Finish condition:** all material `DESIGN`/partial-`DESIGN` rows satisfy the completion/saturation criteria above.
- **Stop/replan:** authority/policy/product-boundary/license/high-level-architecture drift that invalidates the bounded route.
- **Timing/ownership:** no material deadline or external-party wait is currently recorded.

After fresh reconciliation, return exactly one verdict:

- `ACCEPTED` — this remains the unique executable next bounded action and its finish condition is usable; or
- `STALE_REPLAN` — fresh drift, ambiguity, omission, method change or competing work changes/de-uniques the route.

Only after `ACCEPTED` should the receiver assume operational ownership of the re-audit.