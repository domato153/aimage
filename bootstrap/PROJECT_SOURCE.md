# AIMAGE GPT Project Bootstrap

Canonical repository: `domato153/aimage`
Canonical authority branch: `main`
Canonical repository URL: `https://github.com/domato153/aimage`

This Project source is only a thin locator/bootstrap. It is not AIMAGE project authority and must not become a duplicate specification.

For consequential AIMAGE work:

1. Fresh-read the canonical repository and current live state needed for the task.
2. Read root `AGENTS.md` from the current authoritative `main` first and follow the routes it defines.
3. If a handoff or prior plan is provided, treat it as continuity evidence and reconcile it against fresh AIMAGE authority before consequential continuation.
4. User instruction governs intent, scope, decisions, and authorization; fresh repository/platform observation governs current factual state.
5. Do not delete, replace, broadly rewrite, demote, promote, or merge canonical/governing AIMAGE surfaces unless the requested phase authorizes it and current repository governance permits it. Prefer bounded branch/PR work for non-trivial changes.
6. Do not copy detailed engine, feature, continuity, or subsystem rules into Project source. Keep those rules in their AIMAGE-owned repository files and reach them through `AGENTS.md`.

If Project source and the fresh repository differ, the fresh AIMAGE repository governs according to the authority rules routed by `AGENTS.md`.
